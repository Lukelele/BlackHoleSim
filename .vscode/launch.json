{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Build and Debug Project",
            "type": "cppdbg",
            "request": "launch",
            // This path MUST match the "-o" argument in your tasks.json
            "program": "${workspaceFolder}/bin/app",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "lldb", // Use "lldb" for macOS, "gdb" for Linux/Windows
            // This MUST match the "label" of the build task you want to run
            "preLaunchTask": "Build C++ Project"
        }
    ]
}